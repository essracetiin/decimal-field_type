{{ asset_add('scripts.js', 'anomaly.field_type.decimal::js/input.js', ['debug']) }}

<input
        type="text"
        class="spinner form-control"
        value="{{ field_type.value }}"
        data-min="{{ field_type.config.min }}"
        data-max="{{ field_type.config.max }}"
        data-step="{{ '0.00001'|number_format(field_type.config.decimals) }}"
        name="{{ field_type.getInputName() }}">
